language: haskell
install:
  - "cabal install --enable-tests --dry-run -v3"
  - "travis_retry cabal install --force-reinstalls --enable-tests"
  - "cabal test"
notifications:
  email:
    recipients:
      - adam@silk.co
    on_success: never
    on_failure: change
